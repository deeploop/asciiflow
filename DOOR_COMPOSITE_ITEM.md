# 複合門組件(Composite Door Item)實作報告

> 對應程式:`client/composite_door.ts`、`client/text_grid.ts`(共用元件)
> 對應測試:`client/composite_door.spec.ts`
> 對應功能:外框 + 門鎖 + 高度計算鏈 + 寬度計算鏈 + 描述文字,一次產生、整體可搬移

---

## 1. 你描述的手動工作流程——核實結果

你寫的三步驟工作流本身完全正確、現在就能用,不需要任何新程式碼:

| 步驟 | 你的描述 | 核實結果 |
|------|----------|----------|
| 建立結構 | Box 工具畫外框 + 文字標籤當門鎖/尺寸/描述 | ✅ 正確,ASCIIFlow 本來就是這樣運作 |
| 整體移動 | 拖曳虛線框「全部包進去」再搬移 | ✅ 正確——而且是**唯一**能把外框+門鎖+尺寸+描述全部一起搬走的方法,已用真實滑鼠操作驗證 |
| 整體縮放 | ASCIIFlow 做不到一鍵縮放,建議分段調整 | ✅ 正確診斷,這是純文字工具的本質限制 |

但你在「門鎖」的寫法裡有一個具體細節,我實際測試後發現**會破壞你自己描述的「整體移動」功能**,值得在動手畫之前先知道。

---

## 2. 一個會讓「點內部整體搬移」失效的細節

你寫:「在框線邊緣輸入一個特殊字元(例如 `[=]` 或 `O`)代表門鎖」。如果照字面**直接把外框的某個字元蓋掉**,例如:

```
┌────┐
│    O   ← 用 O 蓋掉右邊框線
│    │
└────┘
```

我用專案裡真正的框選判斷程式碼(`findBox()`,決定「點框內部→整體選取搬移」這個功能認不認得出這是一個框)實測:

```
border-overwrite lock: findBox from interior -> NULL (box detection broken)
```

**框線只要有一格不是框線字元,ASCIIFlow 就不再認得這是一個框**,「點內部拖曳整體搬移」這個功能會直接失效,而且不會有任何錯誤提示——你會發現點框內部搬移，然後突然整個框「消失」或行為怪異，且不知道為什麼。原因是 `findBox()` 內部的 `verifyPerimeter()` 檢查要求框線四邊**每一格**都必須是框線字元(`─│┌┐└┘...`),`O` 不是框線字元,檢查直接失敗。

**修正方式**:門鎖字元往內縮一格,貼著框線但不覆蓋它:

```
┌────┐
│   O│   ← O 在框內,緊貼右邊框線,但沒有蓋到「│」
│    │
└────┘
```

這樣框線維持完整,`findBox()` 就能正常辨識。

---

## 3. 但這樣還不夠——門鎖也可能「點內部搬移時被留在原地」

修正後我再測了一次,發現另一個更細節的問題:**框線外側**(緊貼但在框線之外)的字元,雖然不會破壞框的辨識,但「點內部整體搬移」這個功能實際搬動的範圍是**框的矩形範圍內部**(`cellsInBox`,即左上到右下這個矩形內的所有內容),框外的東西(不管多近)**不會**跟著移動:

```
測試結果:
adjacent lock: findBox from interior -> FOUND box ...    ← 框線辨識沒問題
是否 lock 在框範圍內（會不會跟著移動）？ false            ← 但不會跟著搬走
```

也就是說,門鎖字元有三種可能位置,各自的移動行為完全不同:

| 門鎖位置 | 框線辨識(能否「點內部搬移」) | 門鎖是否跟著移動 |
|----------|------------------------------|------------------|
| 蓋在框線上 | ❌ 壞掉 | — (整個功能都壞了) |
| 貼在框線外 | ✅ 正常 | ❌ 不會跟著移動,留在原地 |
| **在框線內縮一格** | ✅ 正常 | ✅ 會跟著移動 |

所以我在 `composite_door.ts` 的產生器裡,把門鎖固定放在「框線內縮一格」的位置——這是唯一同時滿足「不破壞框線辨識」和「點內部搬移時會一起走」兩個條件的位置。這不是憑經驗猜的,是先讀了 `draw/entity.ts` 的 `findBox`/`cellsInBox` 原始碼、寫小測試驗證後才定案的設計。

---

## 4. 我做了什麼:`generateCompositeDoor()`——把你的手動流程自動化

你描述的手動流程(畫外框→標門鎖→標尺寸→標描述→选取搬移)完全正確,但每次都要手動排版、手動對齊、手動計算門鎖該留在哪一格才不會撞到描述文字。我把這整套流程寫成一個產生器函式,輸入結構化資料,直接產生排好版、驗證過的完整文字區塊:

```typescript
generateCompositeDoor({
  boxWidth: 14,
  boxHeight: 6,
  widthChain: { parts: [600, "+34"], result: 634 },     // 你範例的寬度計算鏈
  heightChain: { values: [1200, "-4", "-2", 1192] },      // 你範例的高度計算鏈
  lock: { side: "left", rowRatio: 0.85 },
  description: ["[ID: Door-01]", "[Type: Flush]"],
})
```

輸出(已截圖驗證,貼進 ASCIIFlow 完全對齊):

```
         600 +34
           634
     ┌──────────────┐
1200 │              │
  -4 │              │
  -2 │[ID: Door-01] │
1192 │[Type: Flush] │
     │O             │
     │              │
     └──────────────┘
```

**這直接解決了你提到的「無法一鍵縮放」痛點**——不是讓 ASCIIFlow 學會縮放文字(那做不到,純文字工具的本質限制),而是**根本不需要縮放**:換一個尺寸,重新呼叫一次函式就好,不用「先拉大外框、再手動搬移門鎖、再手動拉伸尺寸線」這三個步驟。你原本描述的「分段調整法」變成完全不必要。

---

## 5. 溢出檢查:`checkCompositeDoorOverflow()`——這次測試又抓到一個真實案例

跟門型公式驗證、立面圖產生器一樣的模式:寬度/高度不吻合不會報錯,而是「悄悄畫錯」。這次寫測試時,用預設的門鎖位置(`rowRatio` 預設置中 0.5)搭配兩行置中的描述文字,測出來的結果是:

```
1192 │O             │        ← 修正前
1192 │OType: Flush] │        ← 有 bug 版本:O 蓋掉了描述文字的「[」
```

門鎖預設落在跟描述文字同一列,兩者的欄位剛好沒對齊,`O` 直接蓋掉了 `[Type: Flush]` 的開頭字元。所以加了 `checkCompositeDoorOverflow()`,除了原本就有的「描述太長」「高度鏈超出框高」「寬度公式超出框寬」之外,**新增了「門鎖位置跟描述文字所在列重疊」的檢查**:

```
lock sits on interior row 3, inside the description's row range [2, 4) —
set lock.rowRatio or reflow the description to avoid the lock character
overwriting description text
```

範例規格把 `rowRatio` 調整為 `0.85`(貼近底部,避開置中的描述文字)後,警告清空,如上方截圖所示。

---

## 6. 「整體移動」的完整真相——已用真實滑鼠操作驗證

這是這次驗證花最多力氣的部分,因為單靠肉眼看畫面容易被誤導。我在 app.tsx 額外暴露了幾個測試用的座標轉換函式(`cellToScreen`/`screenToCell`/`getCellValue`,已保留在程式碼中,對未來的自動化測試也有用),然後**真的用滑鼠事件**(不是直接呼叫程式函式)操作了瀏覽器裡執行中的 ASCIIFlow:

### 6.1 點框內部,拖曳搬移

```
點擊框內部空白處 → 拖曳 → 放開
結果:外框 + 門鎖 + 描述文字 一起移動
      高度計算鏈("1200"/"-4"/"-2"/"1192")留在原地
      寬度計算鏈("600 +34"/"634")留在原地
```

這證實了：**只點框內部,不會帶走外部的尺寸標註**——跟你原本猜測的一致,這是 ASCIIFlow 選取機制的固定行為,不是我這個產生器能改變的。

### 6.2 拖曳虛線框「全部包進去」,再搬移

```
拖曳選取範圍涵蓋所有元素 → 再從選取範圍內拖曳搬移
結果(用絕對座標核實,不只是看畫面):
  高度鏈數字「1」原位置 → 變成空格(已搬走)
  高度鏈數字「1」新位置 → 出現「1」(確實搬到新位置)
  外框角落「┌」同步搬移
  門鎖「O」同步搬移
```

四個元素(外框、門鎖、高度鏈、寬度鏈)全部以完全相同的位移量一起移動——這就是你描述的「整體搬移」,而且已經用程式直接讀取畫布儲存格內容核實過,不是憑截圖判斷。

### 6.3 複製貼上(「元件庫」工作流)

這部分**沒有**做瀏覽器層級的自動化驗證——Ctrl+C/V 走的是瀏覽器原生剪貼簿 API,在無頭瀏覽器自動化測試環境裡權限/沙盒限制較多,不易穩定重現。但這是 ASCIIFlow 既有、與這次新功能無關的通用機制(任何選取範圍都能複製貼上,不只是複合門),你原本描述的「畫一個 Master 範本,複製貼上重複使用」的工作流程本身沒有問題,只是這次沒有另外寫自動化測試去重新驗證這個既有功能。

---

## 7. 檔案異動總覽

| 檔案 | 內容 |
|------|------|
| `client/text_grid.ts`(新) | 把 `elevation_drawing.ts` 原本內建的 CJK 安全文字網格(`TextGrid`/`charWidth`/`displayWidth`)拆成獨立共用模組,兩個產生器共用同一套邏輯 |
| `client/composite_door.ts`(新) | `generateCompositeDoor()`、`checkCompositeDoorOverflow()`,含門鎖位置設計的完整技術說明(見檔案開頭註解) |
| `client/composite_door.spec.ts`(新) | 16 項測試,包含直接呼叫 `findBox()`/`cellsInBox()` 核實框線辨識與門鎖移動行為的迴歸測試 |
| `client/app.tsx` | 新增 `cellToScreen`/`screenToCell`/`getSelectionBox`/`getCellValue` 測試橋接函式(供瀏覽器層級的座標精確操作與核實,對未來任何 e2e 測試都有用,非本次專屬) |

---

## 8. 尚未做的事

- **沒有把這個產生器接進工具列 UI**——目前是純函式,用法跟立面圖產生器一樣:呼叫函式拿到文字、貼到畫布。如果你要在工具列上有一個「複合門」按鈕/表單,是下一步的 UI 工作,規模跟之前「立面圖表單 vs Excel 驅動」的討論類似,需要先決定資料怎麼輸入。
- **複製貼上沒有自動化驗證**(理由見第 6.3 節)——如果你需要针對這個具體工作流程的自動化回歸測試,我可以再花時間解決無頭瀏覽器剪貼簿權限的問題。
- **門鎖只支援單一字元、單一位置**——如果你需要更複雜的五金圖示(例如門鎖 + 門把手兩個不同標記),目前的 `CompositeDoorLock` 介面只設計了一個位置,需要的話可以擴充成陣列。
