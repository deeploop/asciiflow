# AI 代理快速定義新門型範本檔案——完整實測流程

> 本報告不是理論說明,是這次對話裡**真的執行過**的一次完整流程紀錄:由我(AI 代理)產生定義檔、程式驗證、抓到一個錯誤、修正、載入瀏覽器、蓋章、截圖存證。所有指令與輸出都是真實執行結果。

---

## 1. 為什麼「AI 代理 + 執行期載入」是目前最快的新增門型方式

專案裡新增門型有三種管道,速度與適用情境不同:

| 方式 | 需要重新編譯? | 誰能做 | 速度 |
|------|---------------|--------|------|
| 改 `client/door_registry.ts`(編譯期) | 需要 | 工程師 | 慢(改檔案 → 建置 → 部署) |
| 手動填寫 JSON/文字定義檔 | 不需要 | 任何會寫 JSON 的人 | 中等(要懂公式語法、自己算寬度) |
| **請 AI 代理直接產生定義檔** | 不需要 | 任何人(只要會描述需求) | **最快**——描述需求、拿到檔案、載入,幾十秒內完成 |

第三種之所以快,是因為「公式語法規則」「寬度不變量」「代號格式限制」這些細節不需要人記住——AI 讀過 `DOOR_TEMPLATE_FORMAT.md` 和 `DOOR_DYNAMIC_LOADING_AI_PROMPT.md` 就能照規則產生,而且**產生完不是「信任 AI 寫對」,是程式本身會驗證,寫錯會被擋下來並告知原因**。下面用一次真實案例示範這個「AI 產生 → 程式驗證 → 錯誤回饋 → AI 修正」的完整迴圈。

---

## 2. 實測案例:一次請求三種新門型

### 2.1 需求(可以直接這樣跟 AI 代理說)

> 幫我定義三種新門型:防火門(FR、Fire Door)、氣密門(QM、Airtight Door)、旋轉門(RT、Revolving Door),存成 JSON 格式的門型定義檔。

### 2.2 AI 代理的第一次產出(`attempt1.json`)

```json
{
  "FR": {
    "name": "防火門",
    "englishName": "Fire Door",
    "row1_formula": "'▣' + '═' * (W-2) + '▣'"
  },
  "QM": {
    "name": "氣密門",
    "englishName": "Airtight Door",
    "row1_formula": "'◆' + '▒' * (W-4) + '◆'"
  },
  "RT": {
    "name": "旋轉門",
    "englishName": "Revolving Door",
    "row1_formula": "'◇' + '╬' * (W-2) + '◇'"
  }
}
```

### 2.3 程式驗證(不是我口頭檢查,是實際跑 `parseDoorRegistryFile()`)

```
$ node validate.cjs attempt1.json
loaded codes: [ 'FR', 'RT' ]
errors:
 - 代號「QM」的公式在寬度 14 時輸出了 12 個字元,必須恰好是 14(見 DOOR_TEMPLATE_FORMAT.md 的寬度不變量)
```

**FR 跟 RT 一次就對,QM 被擋下來了。** 原因是我(身為產生這份檔案的一方)在 QM 的公式裡把 `(W-2)` 誤寫成 `(W-4)`——`◆` 是單字元端帽,兩端只佔 2 格,填充段落應該是 `(W-2)`;`(W-4)` 是雙字元端帽(像 `●├`/`┤●`)才該用的算式。這正是 `DOOR_DYNAMIC_LOADING_AI_PROMPT.md` 裡特別點名警告過的錯誤類型,而它確實在這次的產出裡發生了。

### 2.4 修正並重新驗證

```diff
-    "row1_formula": "'◆' + '▒' * (W-4) + '◆'"
+    "row1_formula": "'◆' + '▒' * (W-2) + '◆'"
```

```
$ node validate.cjs attempt2.json
loaded codes: [ 'FR', 'QM', 'RT' ]
errors:
```

三個都通過了。

### 2.5 載入真實瀏覽器、蓋章、截圖(附圖)

把 `attempt2.json` 透過門面板的 **[load door types]** 真實檔案選擇對話框載入(不是模擬呼叫函式),結果:

- 選單從 8 個按鈕(6 內建 + 之前示範的 DD/RD,若尚未清除)變成新增 `FR 防火門`、`QM 氣密門`、`RT 旋轉門`,自動換行不影響版面。
- 三種門型都成功蓋章:`FR01-IL`、`QM01-IL`、`RT01-IL`,特徵列(`▣════▣`、`◆▒▒▒◆`、`◇╬╬╬◇`)寬度與外框完全對齊。
- 右下角 toast 顯示「loaded 3 door type(s): FR, QM, RT」。

截圖已附上,為實際瀏覽器操作結果。

---

## 3. 這個流程告訴你什麼(重點整理)

### 3.1 「AI 產生的東西不用逐字檢查」——因為程式本身就是檢查者

你不需要懂公式語法、不需要自己算寬度、甚至不需要信任 AI 一次就寫對。**驗證器(`validateDoorConfig`)才是真正的把關者**:任何一筆定義,不管是人手寫還是 AI 產生,都要真的通過「用 `renderDoorLine()` 在寬度 14、15、20、30 各跑一次、輸出長度必須剛好等於該寬度」才會被接受。這次示範裡,錯誤的 QM 定義**在載入瀏覽器之前就已經被攔下**,不會出現「載入後畫面跑版才發現」的情況。

### 3.2 錯誤訊息本身就是給 AI 的修正指令

錯誤訊息「代號「QM」的公式在寬度 14 時輸出了 12 個字元,必須恰好是 14」已經包含修正所需的全部資訊:少了 2 個字元、代號是 QM。把這則訊息連同原始定義檔一起丟回給 AI 代理(不管是同一個對話,還是貼到別的 AI 工具),AI 就能推算出「端帽只佔 1 格,填充算式該是 `(W-2)` 不是 `(W-4)`」並直接修正——這也是為什麼 `DOOR_DYNAMIC_LOADING_AI_PROMPT.md` 特別交代:「如果載入後門型數量比你寫的少,先看瀏覽器主控台(console)的錯誤訊息,不要假設是檔案格式錯了。」

### 3.3 「快」具體指的是什麼

整個「描述需求 → 拿到 JSON → 驗證失敗 → 修正 → 驗證通過 → 載入 → 蓋章成功」的迴圈,這次實測**在同一個對話裡幾個指令就走完**,全程沒有:

- 修改任何 ASCIIFlow 原始碼
- 重新建置(`bazel build`)
- 部署或重新整理頁面(頁面本來就是開著的,載入後立刻生效)

跟前面兩份報告(`DOOR_MENU_QUICK_DEFINE.md` 的「改 `door_registry.ts` + 重新編譯」流程、`DOOR_DYNAMIC_LOADING.md` 的架構說明)相比,這是**目前最快的路徑**,代價是型別安全较弱(執行期代號無法在編譯期檢查,見 `DOOR_DYNAMIC_LOADING.md` 第 6 節),但公式安全性完全不受影響——每一筆都照樣要通過同一套寬度驗證。

---

## 4. 你可以直接複製使用的請求模板

跟任何 AI 代理(這個對話、或貼到別的 AI 工具)這樣說,幾乎都能重現這個流程:

> 幫我定義 N 種新門型:[中文名稱](代號、English Name)、[中文名稱](代號、English Name)……,存成 ASCIIFlow 門型定義檔(JSON 或文字格式皆可)。公式請遵守 `DOOR_TEMPLATE_FORMAT.md` 的語法規則,並在輸出前手動驗算每個公式在 W=14 時的長度是否剛好等於 14。

如果想要 AI 代理自己完成「產生 → 驗證 → 修正」全流程(而不是你自己跑驗證指令),可以加一句:

> 產生後請用專案裡的 `parseDoorRegistryFile()` 實際驗證,有任何一筆被拒絕就自動修正並重新驗證,直到全部通過為止,最後再把最終檔案內容和驗證結果一起給我。

這正是這份報告示範的完整流程——這次是我自己完成的,你也可以把同一套指令套用在未來任何一次新增門型的需求上。

---

## 5. 附錄:本次產生的定義檔(可直接使用)

修正後的 `attempt2.json`(等同 `custom_doors_fire_airtight_revolving.json`)內容:

```json
{
  "FR": {
    "name": "防火門",
    "englishName": "Fire Door",
    "row1_formula": "'▣' + '═' * (W-2) + '▣'"
  },
  "QM": {
    "name": "氣密門",
    "englishName": "Airtight Door",
    "row1_formula": "'◆' + '▒' * (W-2) + '◆'"
  },
  "RT": {
    "name": "旋轉門",
    "englishName": "Revolving Door",
    "row1_formula": "'◇' + '╬' * (W-2) + '◇'"
  }
}
```
