# 實際門片施工圖(Shop Drawing)——圖面判讀 + 樣板實作回報

> 你上傳的 5 張圖是一份真實的玻璃拉門施工圖(立面圖 + 表頭資料 + 局部放大),這份文件先逐項判讀圖面內容,再回報把它變成可重複使用範本(`ElevationSpec`)的實作結果。

---

## 1. 圖面逐項判讀(繁體中文)

### 1.1 表頭資料列

```
二三    信義路四段30巷16號12F    密:6789E    車：B3F-50    高S0905379331    115.03.18安裝(胖)
```

| 欄位 | 判讀 |
|---|---|
| `二三` | 案場/跑單編號,或工班內部代號 |
| `信義路四段30巷16號12F` | 施工地址 |
| `密:6789E` | 大樓門禁/電子鎖密碼(6789 加尾碼E) |
| `車：B3F-50` | 卸貨/施工車輛停放位置:地下三樓50號車位 |
| `高S0905379331` | 聯絡人姓氏「高」+ 手機號碼 0905-379-331 |
| `115.03.18安裝(胖)` | 預計安裝日期(民國115年3月18日,即西元2026年3月18日)+ 負責安裝師傅代號「胖」 |

這一列是**跑單/工班共用的案場資訊**,跟門片本身的規格無關,但實務上一定會印在施工圖最上方,方便工班核對案場。

### 1.2 立面圖(門片正面圖)

| 元素 | 判讀 |
|---|---|
| `廚` / `客` | 門片兩側的空間標示——左邊是廚房、右邊(或說門後方)是客廳,標明「開門方向面對哪個空間」 |
| `J01(吊)(黑)+5T茶玻+分隔把` | 門片編號 **J01**、**吊軌式**(懸吊軌道拉門,非落地軌道)、**黑色**框料、**5T茶玻**(5厘/5mm厚的茶色玻璃)、搭配**分隔把**(門扇中間有一根橫向或縱向的分隔把手,兼具結構分隔與把手功能) |
| `1114+18+7+24` | 門洞總寬度的拆解公式——主體寬1114mm,再加上左右預留縫隙18+7+24mm(公差、收邊料厚度等) |
| `581` / `581` | 每兩片為一組玻璃扇的實際寬度(這是四扇拉門,分兩組,每組581mm寬,兩組共4扇) |
| `J02` | 圖右側標示的**下一個門片編號**——代表這張圖是一整組門(J01、J02...)其中一片,J02 緊接在J01右邊 |
| 高度公式 `2439 / -45 / -10 / 2384` | 由上到下累減:樓層淨高2439mm,扣除軌道盒高度45mm、扣除上下扣件間隙10mm,得到門扇實際高度2384mm |
| `看100`(雙箭頭標註) | 「可視寬度」或「收邊寬度」為100mm的標註,通常標在最外側兩扇的邊緣,代表玻璃扇距離牆邊/門框邊還有100mm的收邊料或視覺可視範圍 |
| `大側暗把手` / `中心至下1000` | 暗把手(嵌入式把手,不外凸)裝在**開啟時活動幅度較大的那一側**(通常是慣用手操作側),把手中心距離地面(或距扇頂,依現場標註習慣)**1000mm** |
| `防晃輪` | 門扇下方的**防止晃動滾輪**——吊軌拉門因為只靠上方軌道懸吊,下方需要一個小滾輪卡在地面溝槽或牆邊,防止門片左右晃動 |
| `不破` | 標示此處**必須使用不易破裂的玻璃或加強片**,通常是把手/鎖具開孔處,或人員經常接觸碰撞的區域 |
| `附:緩軌1671*2 / 台制緩*2` | 附加零件清單——**緩衝軌道**(soft-close track,長度1671mm)2支,**台灣製緩衝器**(soft-close damper)2個。這些是讓拉門滑到底時自動緩速、不會撞擊的五金 |

### 1.3 右下角小圖(牆體交接剖面/局部詳圖)

這張小圖(用斜線 `╲` `╱` 畫出的楔形 + 上方兩個小色塊 + 底部 `595` 標註)是**牆體與門扇軌道交接處的局部剖視圖**——不是完整的「頂視平面圖」,而是**軌道端點或收邊處的斷面詳圖**:兩個小色塊代表五金(可能是軌道端蓋或滾輪座),斜線代表牆體被剖開後露出的斷面(業界慣用的剖面填充線畫法),`595` 是該處到某個基準點(通常是牆角或鄰近結構)的距離。

---

## 2. 實作:把整張圖變成可重複使用的範本

### 2.1 這張圖跟先前已經做好的 `elevation_drawing.ts` 高度重疊

前幾輪對話已經根據類似的參考圖建好了 `generateElevationDrawing()`——廚/客標示、門型規格列、寬度公式、四扇分組尺寸、左側高度公式鏈、看100 收邊標註、把手標註、底部五金分組——**這些欄位當時就是照這張圖的樣子設計的,程式碼裡的測試資料本來就長這樣**。

這次圖面新增的、原本沒有的部分只有 4 項:
1. 最上方的**表頭資訊列**(案場/地址/密碼/車位/聯絡人/安裝日期)
2. **附:緩軌/緩衝器**的附加零件清單
3. **兩個不同的底部標籤**(左邊「防晃輪」、右邊「不破」——原本只支援一個)
4. 右下角的**牆體交接斜線詳圖 + 尺寸**

### 2.2 這次新增的欄位(`ElevationSpec`)

```typescript
headerInfo?: string[];        // 表頭列,陣列每項用 4 個空格隔開顯示
bottomLabelRight?: string;    // 底部右側標籤(原本的 bottomLabel 變成專指左側)
accessories?: {                // 附加零件清單,位置由你指定
  row: number; col: number;
  title?: string; lines: string[];
};
wallDetail?: {                 // 牆體交接斜線詳圖
  row: number; col: number;
  dimension: string | number;
  width?: number; height?: number;   // 楔形大小,選填
};
```

`headerInfo` 刻意設計成**不影響其他任何欄位的座標**——沒有填就完全不佔用任何一行,原本的畫面一格都不會動;有填,只是在最上面多插一行,下面所有內容整體往下移一行,你不需要為了加這一行去手動調整其他座標。

### 2.3 產生結果(用這張圖的實際數字)

```
二三    信義路四段30巷16號12F    密:6789E    車：B3F-50    高S0905379331    115.03.18安裝(胖)
廚              J01(吊)(黑)+5T茶玻+分隔把
客                     1114+18+7+24
      ┬─────────581─────────┬─────────581─────────┬J02
2439  │    ▬▬    │    ▬▬    │    ▬▬    │    ▬▬    │     附
-45   │          │          │          │          │     緩軌1671*2
-10   │          │          │          │          │     台制緩*2
2384  │◄─看100─► │          │          │◄─看100─► │
      │          │          │          │          │
      │          │          │          │          │
      │          │          │          │          │
      │▌大側暗把手           │          │          │
      │中心至下1000         │          │          │
      │          │          │          │          │
      │          │          │          │          │
      │          │          │          │          │
      │          │          │          │          │
      ┴▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬┴▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬┴
      防晃輪                                    不破
                                                          ▬▬
                                                                ▬▬
                                                        ╲          ╱
                                                         ╲        ╱
                                                          ╲      ╱
                                                           ╲    ╱
                                                            595
```

這是**真的貼到 ASCIIFlow 畫布上驗證過**的結果(headless Chromium 實機測試,不是只看程式輸出),24 個原圖欄位(表頭6項、廚/客、門型、公式、581×2、J02、高度鏈4項、看100×2、把手2項、防晃輪、不破、附、緩軌、台制緩、595)全部貼上後逐一比對存在,一個都沒有漏。

### 2.4 範本檔案(可重複使用)

`reference_door_shop_drawing.example.json` 就是這張圖的完整 JSON 規格,搭配新增的 `parseElevationSpecFile()` 載入/驗證函式(跟門型登記檔、框架樹範本檔一樣的模式:JSON 格式有誤或欄位型別不對,會回報清楚的錯誤訊息,不會讓一半資料悄悄消失)。下一片門(J02、J03...)只要複製這份 JSON、改掉門型編號跟尺寸數字就能重新產生,不用每次手動排版。

---

## 3. 測試與驗證結果

- **25 個單元測試**(新增 14 個:表頭列不影響原座標、表頭欄位渲染、雙底部標籤各自定位、附加零件清單定位、牆體詳圖渲染、無這些欄位時乾淨省略共 6 項,加上 JSON 範本解析器的合法/非法輸入共 8 種情境),全數通過。
- **全專案回歸測試**:153 個測試(13 個 spec 檔案)全數通過,沒有任何既有功能被影響。
- **TypeScript 型別檢查**:乾淨(唯一既有錯誤跟 Electron 擴充功能檔案有關,與本次改動無關)。
- **真實瀏覽器驗證**:整張圖(含表頭列)用真實 `paste` 事件貼到畫布上,`getCommittedText()` 讀出來的內容跟產生器輸出完全一致,24 個欄位逐一核對存在。

## 4. 目前狀態與限制

- 這是**產生器 + 範本解析器**,還沒有工具列 UI(沒有「貼上這張圖」的按鈕,也沒有把 JSON 範本檔載入到編輯器內的介面)——目前的使用方式是:改範本 JSON → 呼叫產生器 → 複製輸出貼到畫布。如果你要工具列上有一鍵載入/蓋章的按鈕,請告訴我。
- 右下角的牆體交接詳圖是**示意性的斜線楔形圖案**(固定造型,只有尺寸跟標註文字可調),不是照原圖的五金外形逐一還原——如果你需要更精確的剖面圖案(例如不同的五金形狀、多層牆體),需要再進一步告訴我確切造型。

## 5. 檔案清單(本次新增/修改)

- `client/elevation_drawing.ts`(擴充 `ElevationSpec`,新增 `headerInfo`/`bottomLabelRight`/`accessories`/`wallDetail`/`parseElevationSpecFile`)
- `client/elevation_drawing.spec.ts`(新增 14 個測試)
- `reference_door_shop_drawing.example.json`(這張圖的完整範本檔)
- `DOOR_SHOP_DRAWING_ANALYSIS.md`(本文件)
