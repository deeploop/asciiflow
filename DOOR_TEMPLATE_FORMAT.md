# 門符號範本定義格式(DOOR_REGISTRY)規格書

> 對應程式:`client/doors.ts`(`DOOR_REGISTRY`、`renderDoorLine`、`evalWidthExpression`)
> 對應測試:`client/doors.spec.ts`
> 版本:v2(公式引擎版,取代原本的 `switch`/`if-else` 寫法)

---

## 1. 目的

本文件詳細說明門型符號的「特徵列」如何用一段**公式字串**定義、這段字串的完整語法規則、寬度不變量(width invariant)、以及新增門型時必須遵守的檢查清單。目標是讓非程式背景的人(或 AI)也能安全地新增門型,而不需要碰觸解析器本身。

**核心原則:公式不是拿去 `eval()` 的,而是被一個手寫的小型解析器逐字元驗證與計算。** 任何不符合語法的公式,在渲染時會直接丟出例外(`Error`),不會有「靜默執行了奇怪的東西」這種資安疑慮。

---

## 2. 資料結構

```typescript
interface DoorConfig {
  name: string;          // 中文名稱,如「懸吊門」
  englishName: string;   // 英文名稱,如「Suspension Door」
  row1_formula: string;  // 特徵列公式(見第 3 節語法)
}

const DOOR_REGISTRY: Record<DoorTypeCode, DoorConfig> = {
  SD: { name: "懸吊門", englishName: "Suspension Door",
        row1_formula: "'●' + '─' * (W-2) + '●'" },
  // ... BS, LM, SL, FD, GD
};
```

新增一種門型,只需要在 `DOOR_REGISTRY` 加一個鍵值對,**不需要**修改任何解析邏輯、`doorTemplate()`、`scanDoors()` 或匯出/匯入程式碼(前提是門型代號也要同步加進 `DoorTypeCode` 型別與掃描用的正規表示式,見第 7 節)。

---

## 3. 公式語法完整規則

### 3.1 整體文法(EBNF)

```
formula   ::= segment ("+" segment)*
segment   ::= "'" pattern "'" ("*" expr)?
pattern   ::= 任意不含單引號的字元(可含反斜線等特殊字元)
expr      ::= 一個只由下列 token 組成的四則運算式:
              整數、W(或 w)、+ - * /、括號 ( )
```

用白話說:**一段公式由若干「片段」用 `+` 串接而成**。每個片段是:

- `'字面圖樣'`:原樣輸出。
- `'字面圖樣' * 運算式`:把圖樣重複平鋪、並截斷到「運算式」算出來的長度。

`*` 前後、`+` 前後的空白會被忽略。

### 3.2 「運算式」(`expr`)只能是純算術

`expr` 是以 `W`(符號寬度,傳入的參數,大小寫皆可)為唯一變數的算術式,只允許:

- 整數(`0-9`)
- `W` 或 `w`
- 四則運算子 `+ - * /`
- 括號 `( )`

**沒有其他任何東西**:不能有函式呼叫、不能有其他變數名稱、不能有字串、不能有分號、不能有 `..`。解析器用 regex 先把字串斷成合法 token(`\d+|[Ww]|[+\-*/()]`),再檢查斷詞結果串回去是否等於原始字串(去空白後)——只要有一個字元斷不出合法 token,就整段視為不合法,直接丟出 `invalid width expression`。這是不使用 `eval()`、又能安全計算算術式的關鍵設計。

之後用**遞迴下降解析器**(recursive descent parser)依標準運算子優先序(先乘除、後加減、括號最優先)計算出實際數值,支援負數(前綴 `-`)。

### 3.3 圖樣平鋪(tile)規則

`'pattern' * expr` 的實際輸出由 `tile(pattern, length)` 產生:

- `length = Math.floor(evalWidthExpression(expr, W))`
- 若 `length <= 0` 或 `pattern` 是空字串,輸出空字串(**不會報錯**,是靜默處理)。
- 否則,把 `pattern` 重複到「至少 `length` 字元」後,再裁切到剛好 `length` 字元。

  例:`tile("/\\", 5)` → `"/\\" .repeat(3).slice(0,5)` → `/\/\/`(反斜線在此僅為排版說明,實際字元是單一反斜線 `\`)。

這代表**多字元圖樣**(如 FD 的鋸齒 `/\`)可以直接寫,平鋪器會自動處理奇偶不整除的情況,不需要手動計算重複次數。

### 3.4 片段邊界的判斷(為何 `(W/2+1)` 這種算式是安全的)

公式裡同時有兩種 `+`:一種是**串接片段用的頂層 `+`**,一種是**運算式內部的加法 `+`**(如 `(W-2)*2` 這種算式裡若有加減乘除混合)。解析器用以下 regex 分辨兩者:

```
/'([^']*)'(?:\s*\*\s*((?:[^+']|\+(?!\s*'))+))?/g
```

白話翻譯:「乘數部份」可以吃進任何字元,**除了**單引號(遇到單引號一定是下一段的開始)和「後面緊接著(可有空白)一個單引號的加號」(那種 `+` 一定是頂層串接符號)。所以:

- `"'x' * (W/2+1)"` 中的 `+1` 後面沒有接引號,會被判定為算式內部的加法,留在乘數裡正確計算。
- `"'x' * (W-2) + 'y'"` 中,`(W-2)` 後面的 `+ 'y'` 因為 `+` 後面接了引號,會被判定為頂層串接,正確切成兩段。

新增公式時**不需要理解這段 regex 的細節**,只要記得:圖樣本身不能含單引號;只要保持「一段引號圖樣 + 選擇性的 `* 算式`」這種結構,解析器就能正確切分。

### 3.5 未匹配殘留字元的檢查

解析完所有片段後,程式會把公式字串中「已匹配的片段」全部移除,剩下的內容再去除空白與 `+` 字元——如果還有殘留(代表公式裡有語法解析不到的東西,例如漏打引號、多餘字元),就丟出 `invalid door formula`。這確保了**看起來合法但其實語法錯誤的公式不會被靜默忽略部分內容**。

---

## 4. 寬度不變量(Width Invariant)—— 最重要的設計限制

### 4.1 規則

**`row1_formula` 在任何 `W` 值下,輸出字串長度都必須恰好等於 `W`。**

原因:符號下方的門框(第 2–4 列)寬度固定是 `W`(`┌` + `─`.repeat(W-2) + `┐` 等),特徵列若長度不對,畫面上下兩部分會對不齊。專案裡有一個測試(`client/doors.spec.ts` 的 `"generates rectangular templates for every type and direction"`,以及公式引擎專屬的 `"every registry formula renders at exactly width W"`)會針對**所有門型 × 所有開向 × 多組寬度**檢查這件事,新增門型後**務必確認這個測試依然通過**。

### 4.2 W 的實際範圍

`W` 由標籤長度反推而來,計算方式(`doorTemplate()`):

```
label   = "{代號}{編號:至少2位數}-{開向代號}"     例:"SD01-IL"(7字元)
interior = " " + label + " " + glyph(2字元) + " "  例:" SD01-IL ▼◄ "(12字元)
W        = interior.length + 2                      例:14
```

- 編號 1–99(2 位數):`label` 長度 7,`W = 14`(**這是最小值**)。
- 編號 100–999(3 位數):`label` 長度 8,`W = 15`。
- 依此類推,每多一位數字,`W` 加 1(理論上無上限,但實務上 14–20 已涵蓋絕大多數情境)。

因此**設計公式時,至少要驗證 `W=14` 與 `W≥20` 兩種情況**,並確認所有「圖樣長度」相關的減法(如 `W-2`、`W-4`)在最小 `W=14` 時都不會變成負數或不足以放下所有端點字元——雖然 `tile()` 對負數長度不會報錯(輸出空字串),但這會讓輸出長度不等於 `W`,导致單元測試失敗且畫面跑版。

---

## 5. 現有六種門型公式逐一解析

| 代號 | 中文 | 公式(TypeScript 字串) | 語意拆解 | W=14 時的輸出 |
|------|------|------------------------|----------|----------------|
| SD | 懸吊門 | `"'●' + '─' * (W-2) + '●'"` | 首尾各 1 個圓點(共 2 字元),中間橫線平鋪 `W-2` | `●────────────●` |
| BS | 緩衝懸吊門 | `"'●├' + '─' * (W-4) + '┤●'"` | 首尾各 2 字元端帽(共 4 字元),中間橫線平鋪 `W-4` | `●├──────────┤●` |
| LM | 拉門 | `"'◄' + '═' * (W-1)"` | 開頭 1 個箭頭,其餘全部是雙線軌道(無尾端帽) | `◄═════════════` |
| SL | 推拉門 | `"'◄' + '═' * (W-2) + '►'"` | 首尾各 1 個箭頭(共 2 字元),中間雙線軌道平鋪 `W-2` | `◄════════════►` |
| FD | 折門 | `"'/\\' * W"` | 雙字元鋸齒圖樣直接平鋪滿整個 `W`(平鋪器自動處理裁切) | `/\/\/\/\/\/\/\` |
| GD | 幽靈門 | `"'░' * W"` | 單一淺色網底字元平鋪滿 `W` | `░░░░░░░░░░░░░░` |

**規律**:每個公式都是「若干固定長度的字面端帽(可以是 0 個)」加上「恰好一段負責吸收剩餘寬度的平鋪片段」,平鋪片段的運算式永遠是 `W 減去所有端帽總長度`。只要遵守這個規律,寬度不變量會自動成立。

---

## 6. 錯誤訊息對照表(除錯用)

| 情境 | 錯誤訊息 |
|------|----------|
| 完全沒有合法的引號片段(如漏打引號) | `invalid door formula: ...` |
| 片段之間有解析不到的殘留字元 | `invalid door formula: ...` |
| 運算式裡出現非法字元(字母、函式呼叫、分號、`..` 等) | `invalid width expression: ...` |
| 運算式是空的或格式錯誤(如 `'x' * ` 或 `'x' * ()`) | `invalid width expression: ...` |
| 括號不成對 | `unbalanced parentheses: ...` |

以上例外都是在 `renderDoorLine()` 呼叫時同步丟出(非非同步),方便在開發階段(如 REPL 或單元測試)立刻抓到問題。

---

## 7. 新增門型的標準作業流程(SOP)

1. **決定代號、中英文名稱、視覺意象**——代號需是 2 個大寫英文字母,且尚未被使用。
2. **同步更新型別**:`DoorTypeCode` 加入新代號;`DOOR_LABEL_REGEX`(在 `scanDoors()` 用)與 `TYPE_FIELD`/`LABEL_FIELD`(在匯入解析用)的正規表示式也要加入新代號,否則畫布掃描與 Excel 匯入認不出新門型。
3. **設計公式**:依第 5 節的規律,先決定「端帽」字面內容與長度,再用 `W - 端帽總長度` 當平鋪長度。
4. **手算驗證**:至少手動算一次 `W=14` 與 `W=20`,確認輸出長度確實等於 `W`。
5. **在 `DOOR_REGISTRY` 加入新條目**,並在 `row1_formula` 上方加一行簡短英文註解說明視覺意象(遵循既有六個條目的風格)。
6. **執行測試**:`client/doors.spec.ts` 的 `"every registry formula renders at exactly width W"` 會自動涵蓋新條目(它是遍歷 `Object.values(DOOR_REGISTRY)` 的),不需要手動加測試也能得到寬度安全性的保障;若想額外驗證視覺內容,再另外加一個具體斷言的 `it(...)`。
7. **視覺確認**:在瀏覽器中實際切換到新門型、蓋章看效果(工具列 door 面板會自動列出新門型,因為 `DOOR_TYPES` 是從 `DOOR_REGISTRY` 動態衍生的)。

---

## 8. 安全性設計說明

- **完全不使用 `eval()` 或 `new Function()`**:算術運算式先經過白名單 token 化(只接受數字、`W`/`w`、四則運算子、括號),斷詞結果需與原始字串(去空白後)逐字元相同,任何無法歸類的字元都會讓整個運算式判定為不合法。這代表即使公式字串來自不受信任的來源(例如未來若開放使用者自訂門型),也不會有程式碰撞(code injection)風險——最壞情況只是丟出一個 `Error`。
- **運算子集合刻意精簡**:只支援 `+ - * /` 與括號,沒有比較運算子、邏輯運算子、位元運算子,降低攻擊面,也讓公式的意圖(純粹計算一個平鋪長度)保持單純。
- **靜默截斷而非例外**:平鋪長度 ≤ 0 時回傳空字串而非報錯,是為了讓「暫時寫錯但仍是合法語法」的公式不會讓整個應用程式崩潰,而是呈現出一個明顯跑版的符號(可由寬度測試在開發階段抓到),兼顧開發體驗與執行期穩健性。

---

## 9. 附錄:原始碼位置速查

| 項目 | 位置 |
|------|------|
| `DoorConfig` 介面 / `DOOR_REGISTRY` | `client/doors.ts` 第 23–72 行 |
| 片段切分 regex(`FORMULA_SEGMENT`) | `client/doors.ts` 第 128 行 |
| 算術運算式解析(`evalWidthExpression`) | `client/doors.ts` 第 135–187 行 |
| 圖樣平鋪(`tile`) | `client/doors.ts` 第 190–195 行 |
| 公式主入口(`renderDoorLine`) | `client/doors.ts` 第 206–224 行 |
| 範本組裝(`doorTemplate`) | `client/doors.ts` 第 244–259 行 |
| 對應單元測試 | `client/doors.spec.ts`,`describe("renderDoorLine (formula parser)", ...)` 區塊 |
