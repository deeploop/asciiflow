# 拖曳邊框縮放時,自動連動重繪尺寸標註/標籤/鎖——實作回報

> 對應問題:「可不可以在拖曳邊框縮放做完之後,順便把尺寸標註也一起重畫?」——已實作、已測試、已在真實瀏覽器驗證。因為 AskUserQuestion 選擇題工具再次連續失敗,這次直接採用我建議的方案(即時追蹤已蓋章的複合門實例),原因見下方第 4 節。

---

## 1. 這次解決的問題

之前已經證實過:用 ASCIIFlow 內建的「拖曳邊框線」把複合門的框拉大拉小,框線本身會變,但**框內的鎖、ID 標籤、框外的尺寸公式都不會跟著重新排版**——因為它們只是貼在那裡的純文字字元,不是跟框的寬高綁定計算的資料。

現在拖曳邊框縮放完成之後,如果那個框是**這個瀏覽階段(session)裡用 `door+` 蓋章工具放上去的複合門**,系統會自動:
1. 讀出縮放後框的新寬高
2. 用**原本的公式數值、鎖的方向、ID 標籤內容**(這些都沒變,變的只有框的大小),重新呼叫產生器算出正確排版
3. 清掉舊的畫面內容,貼上重新排版好的新內容

實測畫面(拖曳右邊框往右拉 8 格):

```
縮放前:                          縮放後:
     ┌──────────────┐              ┌──────────────────────┐
2400 │              │         2400 │                      │
2400 │              │         2400 │                      │
     │[ID: Door-01] │              │    [ID: Door-01]     │  ← 標籤重新置中了
     │O             │              │O                     │
     └──────────────┘              └──────────────────────┘
```

標籤從貼齊左邊,變成**正確置中**在新的框寬裡;鎖、尺寸數字(2400、900)都還在,數值完全沒變(框變大不代表門的實際毫米尺寸變了,只是圖面上畫得比較寬)。

---

## 2. 為什麼這件事本來做不到、現在怎麼解決的

ASCIIFlow **沒有物件的概念**——蓋章蓋出來的複合門,一旦貼到畫布上,就只是一堆純文字字元,系統不記得「這幾個字元是同一組複合門、原本的鎖側/公式是什麼」。要在縮放完成之後自動重畫,第一件事就要解決:**怎麼知道這個被拖曳的框是不是複合門、原本的內容是什麼**。

這次採用的做法:**蓋章工具會在放置成功後,把這個實例(公式規格 + 位置)記在瀏覽器分頁的暫存記憶體裡**,用一個「不會被邊框縮放動到的內部錨點」(框內左上角那一格)當作追蹤依據。當你拖曳某個框的邊框時,系統會先確認這個框是不是有對應的追蹤紀錄;縮放做完後,如果有追蹤紀錄,就用**原本記住的公式/鎖側**加上**新量到的框寬高**重新產生一份,取代舊畫面。

**這個追蹤是盡力而為,不是保證**——只要你做了任何會讓追蹤失效的操作(例如整頁重新整理、複製貼上到別的地方、手動編輯字元蓋掉錨點格),這個框就會**安靜地變回一般的 ASCIIFlow 框**,拖曳邊框只會是純粹的縮放,不會有任何錯誤訊息或損壞畫面——不會因為追蹤失效而搞壞你的圖。

---

## 3. 真實瀏覽器驗證

用 headless Chromium 實際操作:

1. 用 `door+` 蓋一個複合門(box 寬14、公式 2400/900)。
2. 切到選取工具,拖曳右邊框往右拉 8 格。
3. 確認:
   - 框確實變寬了(22 格寬,14+8)
   - `[ID: Door-01]` 標籤**重新置中**在新的框寬裡(不是還停在舊位置)
   - 鎖 `O` 還在
   - 尺寸數字 `2400`、`900` **數值不變**

## 4. 測試涵蓋範圍

- `client/draw/composite_door_resize.spec.ts`(5 個測試):拖曳變寬後正確重繪、標籤正確重新置中、**對一般手繪的框(沒有追蹤紀錄)完全不受影響**(確認這個功能不會誤觸發在無關的框上)、**連續縮放兩次仍然正常追蹤**(不會用一次就失效)、縮到太小時不會當掉。
- 全專案回歸測試:**178 個測試全數通過**,沒有任何既有功能被影響。
- TypeScript 型別檢查乾淨。

### 開發過程中抓到的測試撰寫錯誤(誠實記錄)

寫測試的過程中一度以為功能本身有 bug(縮放後標籤/框線亂掉),追查後發現是**測試資料設得不對**:一開始把測試用的框寬設成 10,但 ID 標籤 `[ID: Door-01]` 有 13 個字元,框太窄導致標籤本身就溢出蓋住邊框字元(這正是 `checkCompositeDoorOverflow` 原本就會抓到的溢出情況)——邊框既然本身就不完整,ASCIIFlow 內建的邊框拖曳偵測自然也跟著出錯,跟這次新加的重繪功能無關。改成合理的框寬(14)之後問題消失。另外也抓到一個測試沒有在每個案例之間清空畫布,導致後面的測試疊加在前一個測試留下的內容上——修正後 5 個測試全數穩定通過。

## 5. 檔案清單(本次新增/修改)

- `client/composite_door.ts`(抽出 `compositeDoorBoxOrigin()` 共用計算,供重繪邏輯重複使用,避免跟產生器的排版邏輯兜不起來)
- `client/composite_door_instances.ts`(新增——追蹤已蓋章的複合門實例 + 縮放後重繪邏輯)
- `client/draw/composite_door.ts`(蓋章成功後註冊追蹤紀錄)
- `client/draw/select.ts`(拖曳邊框時偵測是否為追蹤中的實例,縮放完成後觸發重繪)
- `client/draw/composite_door_resize.spec.ts`(新增,5 個測試)
- `DOOR_RESIZE_REGENERATE.md`(本文件)
