# 複合門(door+)範本選單 + 公式尺寸欄位——實作回報

> 對應這次的三個要求:(1) 複合門是否能像簡單門型一樣「載入新範本」、(2) 要有可以快速調整屬性的自訂選單、(3) 高度/寬度尺寸欄位要能輸入多行公式。三項都已實作、測試、並在真實瀏覽器中驗證。

---

## 1. 你原本看到的畫面 vs. 現在的畫面

你貼的是 `door+`(複合門蓋章工具)設定面板——`box w / box h / lock / dimensions / height / width`,這是**手動輸入單一數字**的版本。你問的「載入新範本」指的是它旁邊的簡單門型工具(`door`)已經有的 `[load door types]` 按鈕,而複合門工具當時**完全沒有這個功能**。

現在複合門面板多了一整列新的東西:

```
template: [standard-900] [tall-1000] [double-1200] │ [load templates] [reset templates]
box w: 16    box h: 14    │  lock: [left] [right]
dimensions: on   height: [textarea, 3行] = 2384   width: [textarea, 3行] = 1163
```

---

## 2. 三項要求分別對應的實作

### 2.1 「load new template」——現在複合門也能載入範本檔了

新增 `client/composite_door_registry.ts`,跟簡單門型的 `door_registry.ts` 是同一套架構:內建幾個範本(`standard-900`、`tall-1000`、`double-1200`)+ 執行期(不用重新編譯)載入的自訂範本,兩者合併,自訂範本同名時覆蓋內建的。

範本檔格式是 JSON 陣列,一個物件一個範本:

```json
[
  {
    "name": "FR-fire-door",
    "boxWidth": 18,
    "boxHeight": 12,
    "lockSide": "left",
    "heightFormula": "2439\n-45\n-10",
    "widthFormula": "1114\n+18\n+7\n+24"
  }
]
```

面板上的 `[load templates]` 按鈕就是載入這個檔案的入口(跟 `door` 面板的 `[load door types]` 一樣,點下去會跳出系統的檔案選擇視窗)。一個範本寫錯不會讓整份檔案失敗——那個範本會被跳過並顯示錯誤訊息,其他範本照常載入(跟簡單門型登記檔同一套「部分失敗、不影響其他項目」的邏輯)。

### 2.2 「custom menu... easy adjust property」——選了範本之後,欄位還是可以繼續調整

`template:` 那一列是按鈕選單,點一個範本(例如 `tall-1000`)會把面板下面所有欄位(box 寬高、鎖的方向、高度/寬度公式)**一次填好**,但填好之後**所有欄位都還是活的、可以繼續手動改**——選範本只是「起點」,不是鎖死。這點在瀏覽器測試中特別驗證過:選了 `tall-1000` 之後手動把高度公式改掉,面板上顯示的計算結果、以及最後蓋到畫布上的門,用的都是你改過的新數字,不是範本原本的數字。

### 2.3 「multi line text box with formular」——高度/寬度從單一數字變成公式輸入框

新增 `client/dimension_formula.ts`:輸入框第一行是基準值,後面每一行必須是帶正負號的增減量(例如 `+18`、`-45`),自動加總算出結果——不用自己心算。這跟你之前上傳的施工圖裡「2439 / -45 / -10 / 2384」「1114+18+7+24」的標註方式是同一套邏輯,差別是**這次是使用者自己打公式、系統幫你算總和**,不用自己先算好 2384 再打進去。

面板上的欄位標籤旁邊會即時顯示 `= 計算結果`(例如 `height: = 2384`),打字的當下就能看到答案,不用先蓋章才知道對不對。

---

## 3. 真實瀏覽器驗證(不是只看程式碼)

用 headless Chromium 實際操作整個流程:

1. 切到 `door+` 工具,確認範本選單顯示 3 個內建範本。
2. 點擊 `tall-1000`,確認 `box w` 欄位變成 16(範本設定的數值)。
3. 確認高度欄位顯示 `height: = 2384`、寬度欄位顯示 `width: = 1025`——這兩個數字是從範本的公式(`2439/-45/-10` 跟 `1000/+18/+7`)即時算出來的,不是我手動輸入的。
4. 手動把高度公式改成 `1000/+50/+25`,確認欄位旁的計算結果立刻變成 `= 1075`。
5. 點擊畫布蓋一個門,讀出實際貼到畫布上的文字,確認:
   - 寬度顯示 `1000 +18 +7` / `1025`(沿用範本原本的寬度公式,因為我沒有改它)
   - 高度顯示 `1000` / `+50` / `+25` / `1075`(用我剛剛手動改過的高度公式,計算結果正確)
   - 鎖的位置、自動編號的 `[ID: Door-01]` 都正確
6. 確認 `[load templates]` 按鈕點下去真的會跳出系統檔案選擇視窗。

## 4. 測試與型別檢查

- `client/dimension_formula.spec.ts`:8 個測試,涵蓋真實案例的加總計算(驗證 2439-45-10=2384、1114+18+7+24=1163)、空白行、小數點增減量、格式錯誤時不崩潰但仍回報清楚的錯誤。
- `client/composite_door_registry.spec.ts`:12 個測試,涵蓋內建範本存在、JSON 範本檔的合法/非法輸入(共 6 種情境)、自訂範本覆蓋內建範本、清除自訂範本還原成只剩內建。
- **全專案回歸測試:173 個測試全數通過**,沒有任何既有功能被影響。
- TypeScript 型別檢查乾淨。

## 5. 舊資料相容性

你之前如果已經用過 `door+` 工具、瀏覽器裡存有舊格式的設定(單一數字 `heightValue`/`widthValue`),重新整理頁面後會自動轉換成新的公式欄位格式(舊數字變成公式的第一行),不會遺失設定也不會報錯。

## 6. 檔案清單(本次新增/修改)

- `client/dimension_formula.ts` / `.spec.ts`(公式解析與加總)
- `client/composite_door_registry.ts` / `.spec.ts`(範本登記檔 + 載入器)
- `client/store/index.ts`(設定改成公式欄位、範本登記檔的持久化與版本計數器)
- `client/draw/composite_door.ts`(蓋章工具改用公式解析結果)
- `client/toolbar.tsx` / `client/toolbar.module.css`(範本選單、載入/清除按鈕、多行公式輸入框、即時計算結果)
- `composite_door_templates.example.json`(可直接載入使用的範本檔範例)
- `DOOR_COMPOSITE_TEMPLATE_MENU.md`(本文件)
