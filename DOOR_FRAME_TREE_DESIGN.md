# 複雜樹狀結構(多層子框架)自訂門型範本檔案——設計說明

> 延續 `DOOR_COMPOSITE_ITEM.md` 的複合門組件,這份文件說明如何把「單一箱框」推廣成「多層子框架的樹狀結構」,並定義成可重複使用的範本檔案。

---

## 1. 先講清楚:ASCIIFlow 裡的「樹」是什麼意思

ASCIIFlow **沒有真正的物件階層**。畫面上看到的每一格都只是純文字字元,系統裡沒有「這個框是那個框的子物件」這種資料結構——`findBox()` 只是即時去看「相鄰字元剛好排成一個矩形」就判定為一個框,除此之外沒有任何父子關係的記錄。

所以這裡講的「樹」,**只存在於你的範本檔案裡**,不存在於畫布上。產生器(generator)會讀取這棵樹狀規格,遞迴走訪每一個子框架,把它們的文字**攤平**成同一份純文字區塊,貼到畫布之後,畫布看到的就只是一堆排列整齊的字元——跟你手動一格一格畫出來的結果完全一樣,只是省去手動排版對齊的功夫。

這不是妥協,是這個工具的正確模型——先講清楚這件事,後面的設計才不會誤導你以為畫布本身「記得」誰是誰的子框。

---

## 2. 設計:遞迴式框架規格(Frame Tree)

把單一箱框(`composite_door.ts` 現有的設計)推廣成樹狀,只需要一件事:**一個框架可以包含多個子框架,每個子框架有自己相對於父框架的偏移座標**,而且可以再往下遞迴。

```json
{
  "id": "double-door-assembly",
  "frame": { "width": 32, "height": 9, "label": ["Double Door Assembly"] },
  "children": [
    {
      "offset": { "x": 0, "y": 2 },
      "node": {
        "frame": { "width": 14, "height": 6, "lock": { "side": "right" }, "label": ["[ID: Door-01L]"] }
      }
    },
    {
      "offset": { "x": 16, "y": 2 },
      "node": {
        "frame": { "width": 14, "height": 6, "lock": { "side": "left" }, "label": ["[ID: Door-01R]"] }
      }
    }
  ]
}
```

**產生器(`generateFrameTree(root)`)的運作方式**:

1. 畫根節點自己的框(如果有的話),寫進共用的 `TextGrid`。
2. 對每個子節點:遞迴產生「那個子節點自己的完整子樹」,再把結果貼進父節點的 `TextGrid`,貼的位置就是 `offset`。
3. 最後回傳攤平後的一整份文字——跟專案裡其他產生器(`doorTemplate`、`generateElevationDrawing`、`generateCompositeDoor`)的回傳形式完全一致,可以直接貼到畫布,或做成蓋章工具。

這不是全新的技術,是把 `composite_door.ts` 已經在用的「把鎖/尺寸鏈貼進同一個 `TextGrid`」這套技巧,從「貼幾個固定角色的零件」推廣成「貼任意數量、任意深度的子框架」。

---

## 3. 兩個既有的限制,在樹狀結構下會被放大,不是消失

這兩點是前幾輪**真的用程式碼驗證過**的結論(見 `DOOR_COMPOSITE_ITEM.md`),樹狀結構不會讓它們消失,只會讓「要注意的地方」變多:

### 3.1 每個想要有鎖的子框架,鎖都要「內縮一格」

`findBox()` 的框線完整性檢查、`cellsInBox()` 的「只搬動框內範圍」規則,是**逐框架獨立適用**的——樹裡有幾個子框架想要標鎖,就要對每一個都套用「鎖放在框線內側一格,不蓋住框線、不放框外」這條規則。兩個子框(像上面的左右兩扇門葉)就是兩次獨立的鎖位置設計,不會因為它們是同一棵樹的子節點就自動套用同一個安全位置。

### 3.2 「點內部整體搬移」只會搬動被點到的那一個框

點擊左邊門葉內部拖曳——**只有左邊門葉會動**,父框架(外圍大框)、右邊門葉、任何尺寸標註都不會跟著移動。子框架越多,這個「只搬一個」的行為越容易讓人誤以為「點哪裡都能搬整棵樹」——**要搬動整棵樹,還是得用拖曳虛線框全部框進去**這個通用機制,樹有 5 個子框架,虛線框就要涵蓋這 5 個框架的整個範圍,原理跟前一輪驗證的「搬動整個複合門組件」完全一樣,只是範圍變大。

---

## 4. 快速定義 + 重複使用:跟門型登記檔一樣的模式

要讓這種樹狀組合「快速定義、方便重複使用」,最自然的做法就是延續你已經在用的門型登記檔那套模式:一份可以載入的 `.json` 範本檔,經過驗證(比照 `checkCompositeDoorOverflow` 檢查子框架有沒有超出範圍、重疊)之後,交給產生器使用。

實務上你可以建立一個小型「範本庫」:

- `double-door.json`——雙開門(如上面範例)
- `door-with-transom.json`——門+上亮窗
- `door-with-sidelight.json`——門+側窗

需要用到哪一種,就載入對應的範本檔案,不需要每次重新手動排版。

---

## 5. 目前的實作狀態

**這份文件是設計說明,不是完工回報**——`generateFrameTree()`、遞迴的 `FrameNode` 型別定義、範本檔案的載入器(`parseFrameTreeFile`),目前都還沒有寫。上一輪你選了「Toolbar 蓋章工具」把單一複合門組件(`generateCompositeDoor`)做成了可以點擊蓋章的 `door+` 工具;這次的樹狀多子框架推廣是**在那之上再往前一步**,規模跟當初把 `composite_door.ts` 從無到有做出來差不多大。

因為你回覆的是「continue」而不是明確選項(選擇題工具連續失敗,沒能收到你的選擇),我接下來會按照我建議的做法(方案一:通用遞迴框架樹)動手實作,原因如下:

- 這是三個選項裡**唯一能真正表達「樹」**的做法——延伸既有多面板產生器(`elevation_drawing.ts`)雖然工程量較小,但面板固定排成一列,做不出「上方亮窗 + 下方兩扇門葉」這種真正的巢狀結構,不符合你這次問題裡「tree with multi sub frame」的字面需求。
- 會沿用這個對話裡已經建立、反覆驗證過的基礎設施(`TextGrid`、鎖位置安全規則、溢出檢查模式),不是從零發明新架構。

如果實作方向跟你設想的不同,請直接告訴我調整方向即可,不需要等選擇題工具恢復正常。
